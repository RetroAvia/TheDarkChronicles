{
  "name": "retroavia-dark-chronicles",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "description": "RetroAvia — The Dark Chronicles: platform game a 8 dimensioni con codici sconto reali.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/run.mjs && node test/viewports.mjs && node test/reachability.mjs && node test/data-check.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
